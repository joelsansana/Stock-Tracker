# Streamlit Community Cloud deployment manifest.
#
# Local dev alternatives (preferred when developing locally):
#   pip install -e ".[ui,dev]"      # full extras (tests, jupyter, matplotlib)
#   pip install -e ".[streamlit]"   # all UI extras combined
#
# share.streamlit.io auto-detects this file. It installs the local
# python_stocks package in editable mode (pulling in the runtime deps
# declared in pyproject.toml) plus the UI deps: streamlit, plotly, and
# the Hugging Face backend (transformers + torch CPU build).
#
# The HF backend is what powers the Sentiment Lab's neural scorer.
# On Streamlit Cloud the first model load downloads ~250 MB on top of
# the libraries; budget for a slower first request.

--extra-index-url https://download.pytorch.org/whl/cpu
-e .
streamlit>=1.30
plotly>=5.18
transformers>=4.20.0
torch>=1.9.0