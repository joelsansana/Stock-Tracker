# Core data science
numpy>=1.21.0
pandas>=1.3.0

# Stock data
yfinance>=0.1.70

# Sentiment (textblob is the default backend)
textblob>=0.15.3

# HTTP
requests>=2.25.0

# Progress bars
tqdm>=4.62.0

# Optional extras — install what you need:
#   pip install "python-stocks[transformers]"   # Hugging Face backend
#   pip install "python-stocks[twitter]"        # Twitter API v2 client (tweepy>=4.14)
#   pip install "python-stocks[dev]"            # tests + notebook deps + matplotlib