"""Streamlit UI package for python_stocks.

Pages in ``pages/`` are auto-discovered by Streamlit. ``components/``
holds reusable chart and cache helpers; nothing in here is imported
from the ``python_stocks`` library itself.
"""